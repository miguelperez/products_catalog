<h1><%= t('titles.list', :model => Banner.human_name) %></h1>
<%= link("forms.new", new_admin_banner_path, 'add')%>

<% header = ['is visible?','Name', 'Description', 'Thumb', 'Actions']%>

<% options = {:attributes => [banner_visible_link, 'name', 'description', thumb_proc], :actions => [:show, :edit, :destroy]} %>
<%= render :partial => 'shared/table', :locals => {:header => header, :options => options, :objects => @banners }%>

<%= will_paginate_links @banners %>

<%= link_to_dashboard %>