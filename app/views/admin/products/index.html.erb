<h1><%= t('titles.list', :model => Product.human_name) %></h1>
<%= link("forms.new", new_admin_product_path, 'add')%>

<% header = ['Is visible?', 'Name', 'Description', 'Price', 'Additional Information', 'Actions']%>
<% options = {:attributes => [product_visible_link, 'name', 'description', 'price', 'additional_information'], :actions => [:show, :edit, :destroy]} %>
<%= render :partial => 'shared/table', :locals => {:header => header, :options => options, :objects => @products }%>

<%= will_paginate @products %>

<%= link_to_dashboard %>